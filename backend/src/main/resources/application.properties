# === Application Name ===
spring.application.name=backend

# === MongoDB Atlas Configuration ===
spring.data.mongodb.uri=mongodb+srv://Didou:mongoatlas@cluster0.d5botz9.mongodb.net/myappdb?retryWrites=true&w=majority
spring.data.mongodb.database=myappdb

# === File Upload Static Resource Mapping ===
spring.web.resources.static-locations=file:uploads/

# === Multipart Settings (important for file upload) ===
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# === Logging (optional for debug) ===
logging.level.org.springframework.web=INFO
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG

# === Port Configuration (optional) ===
server.port=8080

# === Firebase Storage Configuration ===
firebase.credentials.file=/etc/secrets/firebase-service.json
firebase.bucket=buckett_1_test

